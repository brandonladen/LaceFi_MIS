<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Subscription - Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>New Subscription</h1>
            <div>
                <a href="/home" class="btn btn-outline-primary me-2">Home</a>
                <a href="/subscriptions" class="btn btn-outline-secondary">View All</a>
            </div>
        </div>

        <!-- Add New Subscription Form -->
        <div class="card">
            <div class="card-header">
                <h4>Subscription Details</h4>
            </div>
            <div class="card-body">
                <form action="/subscriptions" method="POST">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Name</label>
                            <input type="text" class="form-control" name="name" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Phone Number</label>
                            <input type="text" class="form-control" name="phone_number" required>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Payment Code</label>
                            <input type="text" class="form-control" name="payment_code" required>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Amount (KES)</label>
                            <select class="form-control" name="amount" required>
                                <option value="250">250 (Weekly)</option>
                                <option value="800">800 (Monthly)</option>
                            </select>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Payment Date</label>
                            <input type="datetime-local" class="form-control" name="payment_date" required>
                        </div>
                    </div>
                    <!-- New fields for router details -->
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Router Name (e.g., Lace-Fi#01)</label>
                            <input type="text" class="form-control" name="router_name" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Router Location (e.g., Victoria GHH, Room 23)</label>
                            <input type="text" class="form-control" name="router_location" required>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary">Add Subscription</button>
                </form>                
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>